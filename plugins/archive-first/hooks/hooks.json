{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "COMMAND=$(jq -r '.tool_input.command // empty'); if echo \"$COMMAND\" | grep -qE '(rm|rmdir|unlink).*archived'; then jq -n '{hookSpecificOutput:{hookEventName:\"PreToolUse\",permissionDecision:\"deny\",permissionDecisionReason:\"Blocked: destructive command targeting archived/ directory. Use /archive-first:unlock to remove protection first.\"}}'; else exit 0; fi"
          }
        ]
      },
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "FILE_PATH=$(jq -r '.tool_input.file_path // empty'); if echo \"$FILE_PATH\" | grep -qE '/archived/'; then jq -n '{hookSpecificOutput:{hookEventName:\"PreToolUse\",permissionDecision:\"deny\",permissionDecisionReason:\"Blocked: cannot write to archived/ directory. Use /archive-first:unlock to remove protection first.\"}}'; else exit 0; fi"
          }
        ]
      },
      {
        "matcher": "Edit",
        "hooks": [
          {
            "type": "command",
            "command": "FILE_PATH=$(jq -r '.tool_input.file_path // empty'); if echo \"$FILE_PATH\" | grep -qE '/archived/'; then jq -n '{hookSpecificOutput:{hookEventName:\"PreToolUse\",permissionDecision:\"deny\",permissionDecisionReason:\"Blocked: cannot edit files in archived/ directory. Use /archive-first:unlock to remove protection first.\"}}'; else exit 0; fi"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "for dir in $(find . -maxdepth 3 -type d -name 'archived' 2>/dev/null); do find \"$dir\" -type f -exec chflags uchg {} + 2>/dev/null; chflags nouchg \"$dir\" 2>/dev/null; done; exit 0"
          }
        ]
      }
    ]
  }
}
