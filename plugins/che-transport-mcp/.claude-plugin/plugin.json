{
  "name": "che-transport-mcp",
  "version": "0.2.2",
  "binaryVersion": "0.2.1",
  "description": "Taiwan TDX transport MCP — 23 tools across Rail / Bus / Bike / Air / Maritime / Traffic / Parking. Wraps PsychQuant/che-transport-mcp binary with version-aware auto-download (release-pinned + sha256 sidecar), 3-tier cache TTL (24h static / 1h timetable / 0s live), parallel cross-system station search, NSQL confirmation discipline for ambiguous queries. Built-in `CheTransportMCP --setup` subcommand (getpass-hidden secret prompt, shared keychain code with --check-auth). v0.2.2 shell: setup-tdx skill now hand-holds first-time users with no TDX account through registration + API-key retrieval before launching --setup.",
  "author": {
    "name": "Che Cheng"
  },
  "license": "MIT",
  "keywords": [
    "mcp",
    "taiwan",
    "transport",
    "tdx",
    "rail",
    "bus",
    "youbike",
    "flight",
    "parking",
    "traffic"
  ]
}
