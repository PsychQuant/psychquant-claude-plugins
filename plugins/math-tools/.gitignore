# Local-only reference materials — do not commit textbooks / PDFs / other
# user-side library content into marketplace git history.
references/*.pdf
references/*.epub
references/*.mobi

# Python bytecode generated when the bundled scripts/ run
__pycache__/
*.pyc
# macOS
.DS_Store
